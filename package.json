{
  "name": "my-dashboard-monorepo",
  "private": true,
  "version": "1.0.0",
  "workspaces": {
    "packages": [
      "frontend",
      "mobile",
      "shared"
    ]
  },
  "scripts": {
    "build:shared": "pnpm --filter @my-dashboard/shared build",
    "dev": "pnpm --filter @my-dashboard/shared watch & pnpm --filter frontend dev"
  },
  "trustedDependencies": [
    "@biomejs/biome"
  ]
}
